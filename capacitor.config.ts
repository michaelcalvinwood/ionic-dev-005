import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-dev-005',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
