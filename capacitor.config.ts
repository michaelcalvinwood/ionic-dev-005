import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.michaelcalvinwood.memories',
  appName: 'Memories',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
